server:
  port: 10280
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: news
    password: news1234
    url: jdbc:mysql://localhost:3306/news?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&useSSL=true
  http:
    encoding:
      charset: UTF-8
  jpa:
    hibernate:
      ddl-auto: update
  # Thymeleaf 模板热启动
  thymeleaf:
    cache: false
